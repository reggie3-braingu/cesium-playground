import { renderHook } from '@testing-library/react-hooks'
import { {{formattedHookName}} } from '.'

describe('{{formattedHookName}} test', () => {
    /**
    Use for hooks that need to connect to the Redux store
    it('should run a test', () => {
       const { result } = renderHook(() => {{formattedHookName}}(), {
            wrapper: ({ children }) => <Provider store={store}>{children}</Provider>
        })
    })
    */
    /**
    Use for hooks that do not need to connect to the Redux store
    it('should run a test', () => {
       const { result } = renderHook(() => {{formattedHookName}}())
    })
    */
    
})
